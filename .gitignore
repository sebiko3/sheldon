node_modules/
dist/
*.log
.DS_Store

# Mission state lives in .missions/ in target repos, not in this plugin repo.
# But we still ignore it in case someone runs the plugin against itself for testing.
.missions/

# Per-environment brain observations stay local; seed.jsonl ships with the plugin.
.sheldon/brain/entries.jsonl

# Claude Code writes a per-user permissions cache here. Not project state.
.claude/settings.local.json
