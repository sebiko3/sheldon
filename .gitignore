node_modules/
dist/
*.log
.DS_Store

# Mission state lives in .missions/ in target repos, not in this plugin repo.
# But we still ignore it in case someone runs the plugin against itself for testing.
.missions/
